{#- Copyright © 2025 The Meson Development Team
   SPDX-License-Identifier: Apache-2.0
-#}
{% import 'buckets.tmpl' as buckets %}
cc_defaults {
    name: "{{ target.name }}",
    {{- buckets.render_bucket_property(target.cflags.buckets.get('common', {}).get('', []), 'cflags', 0) }}
    {{- buckets.render_bucket_property(target.cppflags.buckets.get('common', {}).get('', []), 'cppflags', 0) }}
    {{- buckets.render_target_blocks(target, 0) }}
}
