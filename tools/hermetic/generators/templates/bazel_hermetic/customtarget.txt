{# Copyright © 2024 The Meson Development Team
   SPDX-License-Identifier: MIT
#}
genrule(
  name = "{{ target.name }}",
{%- if target.srcs %}
  srcs = [
  {%- for src in target.srcs %}
    "{{ src }}",
  {%- endfor %}
  ],
{%- endif %}
  outs = [
  {%- for out in target.out %}
    "{{ out }}",
  {%- endfor %}
  ],
  tools = [
  {%- for tool in target.tools %}
    "{{ tool }}",
  {%- endfor %}
  ],
  cmd = "{{ target.cmd | wordwrap(80, wrapstring='" + 
        "') }}",
)
